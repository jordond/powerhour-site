import './main.scss';

/* eslint indent: 0 */
const template = [
  '<div>',
    '<header></header>',
  '</div>'
].join('');

const mainComponent = {
  bindings: {},
  template
};

export default mainComponent;

import './main.scss';

/* eslint indent: 0 */
const template = [
  '<div>',
    '<header class="section"></header>',
    '<highlights class="section"></highlights>',
  '</div>'
].join('');

const mainComponent = {
  bindings: {},
  template
};

export default mainComponent;
